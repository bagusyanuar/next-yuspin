import SecTitle from './section-title'
import SecSubTitle from './section-sub-title'

export interface SectionTitleProps { text: string, className?: string }
export interface SectionSubTitleProps { text: string, className?: string }

export const SectionTitle = SecTitle
export const SectionSubTitle = SecSubTitle